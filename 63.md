63. What is GIL?

The Global Interpreter Lock (GIL) is a mechanism in Python that allows only one thread to execute Python bytecode at a time.

Purpose:

Prevent memory corruption

Simplify memory management