4. What is a Dynamically Typed Language?

Python is a dynamically typed language, meaning the data type of a variable is determined at runtime.
You do not need to declare the type of a variable.

Example concept:

x = 10 → integer

x = "Hello" → string

The variable can change type during execution.